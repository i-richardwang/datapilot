#!/bin/sh
# datapilot — agent-facing CLI wrapper.
#
# Dispatches to the unified CLI entry at $DATAPILOT_CLI_ENTRY.
export DATAPILOT_CLI_JSON_ONLY="${DATAPILOT_CLI_JSON_ONLY:-1}"
exec "${DATAPILOT_BUN:-bun}" run "${DATAPILOT_CLI_ENTRY}" "$@"
